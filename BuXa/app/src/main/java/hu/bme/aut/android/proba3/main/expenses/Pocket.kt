package hu.bme.aut.android.proba3.main.expenses

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle


class Pocket : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        //val binding: ActivityPocketBinding = DataBindingUtil.setContentView(this, R.layout.activity_pocket)
        //val binding: UjBinding = DataBindingUtil.setContentView(this, R.layout.uj)


        /*val pluszGomb: Button =findViewById(R.id.button)

        val pluszGombClickListener = View.OnClickListener {
            if (binding.linieEditText.visibility == View.VISIBLE) {
                binding.linieEditText.visibility = View.GONE
            } else {
                binding.linieEditText.visibility = View.VISIBLE
            }
        }

        pluszGomb.setOnClickListener(pluszGombClickListener)
*/

    }
}